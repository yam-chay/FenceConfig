/** Barrel — the real definition moved to geometry/spacerOptions.ts so the closure solver can use it without geometry depending on scene. No import elsewhere needs to change. */
export type { SpacerOption } from '../geometry/spacerOptions';
export { SPACER_OPTIONS } from '../geometry/spacerOptions';